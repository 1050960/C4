<source>
  # ”казываем метод прин€ти€ логов и порт
  @type http
  port 8888
</source>

<filter test1>
  # »спользу€ плагин grep 
  @type grep
  # »сключаем все сообщени€ в поле source которых содержитьс€ localhost
  <exclude>
    key source
    pattern /localhost/
  </exclude>
</filter>

# записываем все в файл
<match test1>
  @type file
  path /tmp/fluent_access
</match>